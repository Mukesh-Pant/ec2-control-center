import { ScreenPlaceholder } from './ScreenPlaceholder';

export default function MyServersScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Provisioning"
      title="My Servers"
      sub="Provision and manage dedicated cloud servers. Choose a template or configure manually."
      emptyIcon="Monitor"
    />
  );
}
