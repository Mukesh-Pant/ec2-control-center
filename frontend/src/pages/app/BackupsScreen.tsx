import { ScreenPlaceholder } from './ScreenPlaceholder';

export default function BackupsScreen() {
  return (
    <ScreenPlaceholder
      eyebrow="Protection"
      title="Backups"
      sub="Snapshots and restores across your fleet."
      emptyIcon="HardDriveUpload"
    />
  );
}
